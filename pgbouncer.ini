[databases]
contacts-sync = host=db port=5432 dbname=contacts-sync user=myuser password=mypassword

[pgbouncer]
listen_addr = *
listen_port = 6432
auth_type = md5
auth_file = /etc/pgbouncer/userlist.txt
pool_mode = transaction
max_client_conn = 100
default_pool_size = 20